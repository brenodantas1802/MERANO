"use client";

import { useState } from "react";
import Link from "next/link";
import { ArrowLeft, ArrowRight } from "lucide-react";
import { useCart } from "@/components/cart-provider";
import { SiteHeader } from "@/components/site-header";
import { formatPrice } from "@/lib/products";

function CheckoutContent() { const { total } = useCart(); const [sent, setSent] = useState(false); if (sent) return <main className="mx-auto max-w-360 px-6 py-24 md:px-12"><p className="sans mb-5 text-[10px] uppercase tracking-[.2em] text-[var(--muted)]">Pedido recebido</p><h1 className="display max-w-3xl text-7xl md:text-9xl">Até já.</h1><p className="mt-8 max-w-lg text-2xl leading-snug">A equipe Merano vai confirmar os detalhes da sua peça e combinar o próximo passo com você.</p><Link href="/" className="sans mt-10 inline-flex items-center gap-2 border-b border-[var(--ink)] pb-2 text-[11px] uppercase tracking-[.15em]">Voltar para o início <ArrowRight size={14} /></Link></main>; return <main className="mx-auto max-w-360 px-6 pb-24 md:px-12"><p className="sans mb-5 text-[10px] uppercase tracking-[.2em] text-[var(--muted)]">Último passo</p><h1 className="display text-7xl md:text-9xl">Checkout.</h1><div className="mt-14 grid gap-14 md:grid-cols-[1fr_340px]"><form onSubmit={(event) => { event.preventDefault(); setSent(true); }} className="max-w-xl space-y-8"><label className="sans block text-[10px] uppercase tracking-[.14em]">Nome completo<input required className="mt-3 block w-full border-b border-[var(--ink)] bg-transparent py-3 outline-none" /></label><label className="sans block text-[10px] uppercase tracking-[.14em]">E-mail<input required type="email" className="mt-3 block w-full border-b border-[var(--ink)] bg-transparent py-3 outline-none" /></label><label className="sans block text-[10px] uppercase tracking-[.14em]">Telefone<input required className="mt-3 block w-full border-b border-[var(--ink)] bg-transparent py-3 outline-none" /></label><div className="grid gap-8 md:grid-cols-2"><label className="sans block text-[10px] uppercase tracking-[.14em]">Rua e número<input required className="mt-3 block w-full border-b border-[var(--ink)] bg-transparent py-3 outline-none" /></label><label className="sans block text-[10px] uppercase tracking-[.14em]">CEP<input required className="mt-3 block w-full border-b border-[var(--ink)] bg-transparent py-3 outline-none" /></label></div><div className="grid gap-8 md:grid-cols-2"><label className="sans block text-[10px] uppercase tracking-[.14em]">Cidade<input required className="mt-3 block w-full border-b border-[var(--ink)] bg-transparent py-3 outline-none" /></label><label className="sans block text-[10px] uppercase tracking-[.14em]">Estado<input required className="mt-3 block w-full border-b border-[var(--ink)] bg-transparent py-3 outline-none" /></label></div><button className="flex w-full items-center justify-between bg-[var(--ink)] px-5 py-4 text-white"><span className="sans text-[11px] uppercase tracking-[.15em]">Enviar pedido para confirmação</span><ArrowRight size={16} /></button></form><aside className="border-t border-[var(--ink)] pt-5"><div className="flex justify-between text-xl"><span>Estimativa</span><span>{formatPrice(total)}</span></div><p className="sans mt-5 text-[10px] uppercase leading-relaxed tracking-[.1em] text-[var(--muted)]">Não cobramos online nesta fase. O fechamento e os detalhes de produção são confirmados manualmente pela equipe.</p></aside></div><Link href="/carrinho" className="sans mt-12 flex items-center gap-2 text-[10px] uppercase tracking-[.15em] text-[var(--muted)]"><ArrowLeft size={14} /> Voltar ao carrinho</Link></main>; }
export default function CheckoutPage() { return <><SiteHeader /><CheckoutContent /></>; }
