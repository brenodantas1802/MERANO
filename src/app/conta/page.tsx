"use client";
import { useState } from "react";
import Link from "next/link";
import { SiteHeader } from "@/components/site-header";
export default function AccountPage() { const [created, setCreated] = useState(false); return <main><SiteHeader /><div className="mx-auto max-w-md px-6 pb-24 pt-16"><p className="sans mb-5 text-[10px] uppercase tracking-[.2em] text-[var(--muted)]">Área do cliente</p><h1 className="display text-7xl">Minha<br /><i>conta.</i></h1>{created ? <div className="mt-12 border-y border-[var(--line)] py-8"><p className="text-2xl">Cadastro recebido.</p><p className="sans mt-3 text-xs text-[var(--muted)]">A conta ficará disponível quando o banco e a autenticação estiverem conectados.</p></div> : <form onSubmit={(event) => { event.preventDefault(); setCreated(true); }} className="mt-12 space-y-7"><label className="sans block text-[10px] uppercase tracking-[.14em]">Nome<input required className="mt-3 block w-full border-b border-[var(--ink)] bg-transparent py-3 outline-none" /></label><label className="sans block text-[10px] uppercase tracking-[.14em]">E-mail<input required type="email" className="mt-3 block w-full border-b border-[var(--ink)] bg-transparent py-3 outline-none" /></label><label className="sans block text-[10px] uppercase tracking-[.14em]">Senha<input required minLength={8} type="password" className="mt-3 block w-full border-b border-[var(--ink)] bg-transparent py-3 outline-none" /></label><button className="w-full bg-[var(--ink)] px-5 py-4 text-left font-sans text-[11px] uppercase tracking-[.15em] text-white">Criar conta</button></form>}<Link href="/carrinho" className="sans mt-10 block text-[10px] uppercase tracking-[.14em] text-[var(--muted)]">Continuar como visitante no checkout</Link></div></main>; }
